Produce the Edit Decision List for this raw clip. Watch the attached video in full before deciding.

## USER GUIDANCE

{{USER_GUIDANCE}}

## CORRECTED TRANSCRIPT

Authoritative script — do NOT re-transcribe. Do NOT emit captions.

```json
{{TRANSCRIPT_JSON}}
```

## CATALOG

Choose transition_id, template_id, and sfx asset_id values **only** from this catalog.
All SFX go in the top-level `sfx[]` array only.

```json
{{CATALOG_JSON}}
```

Return the complete EDL JSON per your system instructions.
